apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gofer-claim
  namespace: grader-k8-namespace
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4Gi
  selector:
    matchLabels:
      name: gofer-volume