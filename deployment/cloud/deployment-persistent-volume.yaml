apiVersion: v1
kind: PersistentVolume
metadata:
  name: otter-volume-dev
  labels:
    name: otter-volume
spec:
  capacity:
    storage: 4Gi
  storageClassName: standard
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  mountOptions:
    - hard
    - nfsvers=4.1
  nfs:
    path: /data
    server: |-
      10.128.0.37
